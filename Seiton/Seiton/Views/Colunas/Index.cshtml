@model IEnumerable<Seiton.Models.Colunas>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nome_coluna)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.cor_coluna)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.quant_tarefas)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Projeto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nome_coluna)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cor_coluna)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.quant_tarefas)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Projeto.nome_projeto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
